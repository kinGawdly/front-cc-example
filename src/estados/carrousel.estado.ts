// src/estados/carrousel.estado.ts
export interface CarrouselEstado {
    // Define aquí los campos del estado del carrusel.
    // Por ejemplo:
    // imagenes: string[];
    // Si aún no sabes qué campos incluir, puedes dejarlo vacío por ahora.
}

export default CarrouselEstado;