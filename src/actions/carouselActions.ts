// src/actions/carouselActions.ts
import { createAction } from '@reduxjs/toolkit';

// Assuming carouselData has a type/interface defined, replace `any[]` with the correct type/interface.
export const fetchCarouselData = createAction<any[]>('FETCH_CAROUSEL_DATA');
